'use client';

import React from 'react';

const PreviewPlaceholder = () => {
  return (
    <div className="flex-1 flex items-center justify-center bg-white text-gray-500 text-2xl font-bold">
      Preview boutique (à implémenter)
    </div>
  );
};

export default PreviewPlaceholder;